#include <iostream>
using namespace std; //now do not need to use std everywhere

int main() {
    int x;
    cin >> x;
    cout << "Value of x: " << x << endl;
    int y, z;
    cin >> y >> z;
    cout << "Value of y " << y << " and value of z " << z << endl; 
    return 0;
}